import React from 'react';
import ImageCarousel from "./ImageCarousel"; // Import the ImageCarousel component

const Home = () => {
    return (
        <div className="py-8">
            <ImageCarousel />
        </div>
    );
};

export default Home;
